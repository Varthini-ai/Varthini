# Read the number of test cases
t = int(input())

for _ in range(t):
    # Read the starting time X
    x = int(input())
    
    # If he starts at X, he finishes at X + 3.
    # We need X + 3 <= 10, which simplifies to X <= 7.
    if x <= 7:
        print("Yes")
    else:
        print("No")# cook your dish here
